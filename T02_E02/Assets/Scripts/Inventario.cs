using UnityEngine;
using System.Collections;

public class Inventario : MonoBehaviour {

    //declaramos e inicializamos una propiedad tipo bool con el nombre de llave, esta nos servira para saber si hemos cogido la llave
	//declaramos e inicializamos una propiedad tipo bool con el nombr de boton, que nos servira para saber si hemos pulsado el boton.
	public bool llave = false;
    public bool boton = false;
}
